

import 'package:url_launcher/url_launcher.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: const HomePage(),
      debugShowCheckedModeBanner: false,
      theme: ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: Colors.purpleAccent),
  useMaterial3: true,
   ) );
  }
}

class HomePage extends StatefulWidget {
  const HomePage({super.key});

  @override
  State<HomePage> createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
  Future<void> _launchUrl(String url) async {
    final Uri uri = Uri.parse(url);
    if (!await launchUrl(uri, mode: LaunchMode.externalApplication)) {
      throw 'Could not launch $url';
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: 
       Container(
  decoration: const BoxDecoration(
    image: DecorationImage(
      image: NetworkImage('https://images.unsplash.com/photo-1557683304-673a23048d34?ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxzZWFyY2h8MTB8fGdyYWRpZW50JTIwYmFja2dyb3VuZHxlbnwwfHwwfHx8MA%3D%3D&fm=jpg&q=60&w=3000'),
      fit: BoxFit.cover,
    ),
  ),
  
     
      child: SingleChildScrollView(
        child: Padding(
          padding: const EdgeInsets.all(24.0),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              const SizedBox(height: 2),
              Center(
                child: Column(
                  children: [
                    CircleAvatar(
                      radius: 80,
                      foregroundColor: Colors.blue.shade300,
                      backgroundImage:
                          const NetworkImage('https://picsum.photos/id/237/200/200'),
                    ),
                    const SizedBox(height: 30),
                    const Text(
                      'John Doe',
                      style: TextStyle(
                        fontWeight: FontWeight.bold,
                        fontSize: 32,
                      ),
                    ),
                    const SizedBox(height: 1),
                    const Text(
                      'Lorem Ipsum Dolor Sit Amet',
                      style: TextStyle(
                        fontSize: 16,
                        fontWeight: FontWeight.bold,
                      ),
                    )
                  ],
                ),
              ),

              // --- SOCIAL ICONS ROW ---
              Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  IconButton(
                    onPressed: () => _launchUrl('https://yourwebsite.com'),
                    icon: const Icon(Icons.web),
                    color: Colors.blue.shade300,
                    iconSize: 50,
                  ),
                  IconButton(
                    onPressed: () => _launchUrl('mailto:someone@example.com'),
                    icon: const Icon(Icons.mail),
                    color: Colors.blue.shade300,
                    iconSize: 50,
                  ),
                  IconButton(
                    onPressed: () => _launchUrl('tel:+1234567890'),
                    icon: const Icon(Icons.phone),
                    color: Colors.blue.shade300,
                    iconSize: 50,
                  ),
                ],
              ),

              const SizedBox(height: 20),
              const Text(
                'About Me',
                style: TextStyle(
                  fontWeight: FontWeight.bold,
                  fontSize: 40,
                ),
              ),
              const SizedBox(height: 12),
              const Text(
                'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Cras sagittis lobortis nisi, dignissim mattis ante molestie a. Ut elit enim, mollis quis massa eget, facilisis eleifend diam.',
                style: TextStyle(
                  fontSize: 16,
                  height: 1.5,
                  color: Colors.white70,
                ),
              ),
              const SizedBox(height: 40),

              // --- MY SKILLS START ---
             Row(
  mainAxisSize: MainAxisSize.min,
  children: const [
    Icon(
      Icons.engineering,
      color: Colors.tealAccent,
      size: 26,
    ),
    SizedBox(width: 8),
    Text(
      'My Skills',
      style: TextStyle(
        fontSize: 22,
        fontWeight: FontWeight.bold,
        color: Colors.white,
      ),
    ),
  ],
),
              const SizedBox(height: 16),
              Wrap(
                runSpacing: 8,
                spacing: 8,
                children: const [
                  Chip(
                    label: Text('Flutter', style: TextStyle(color: Colors.white)),
                    backgroundColor: Colors.lightBlue,
                  ),
                  Chip(
                    label: Text('Dart', style: TextStyle(color: Colors.white)),
                    backgroundColor: Colors.teal,
                  ),
                  Chip(
                    label: Text('C Language', style: TextStyle(color: Colors.white)),
                    backgroundColor: Colors.lightBlue,
                  ),
                  Chip(
                    label: Text('Git', style: TextStyle(color: Colors.white)),
                    backgroundColor: Colors.teal,
                  ),
                  Chip(
                    label: Text('Docker', style: TextStyle(color: Colors.white)),
                    backgroundColor: Colors.lightBlue,
                  ),
                ],
              ),
              const SizedBox(height: 20),
              const Divider(color: Colors.white24),
              const SizedBox(height: 20),

             Row(
  mainAxisSize: MainAxisSize.min,
  children: 


   const [
    Icon(
      Icons.music_note_outlined,
      color: Colors.tealAccent,
      size: 26,
    ),
    SizedBox(width: 8), // spacing between icon and text
    Text(
      'My Fav Song',
      style: TextStyle(
        fontSize: 22,
        fontWeight: FontWeight.bold,
        color: Colors.white,
      ),
    ),
  ],
),
  
             
             
              const SizedBox(height: 20),

              // --- SONG BOX ---
              SongBox(launchUrlCallback: _launchUrl),
            ],
          ),
        ),
      ),
    ));
  }
}

// --- SONG BOX WIDGET ---
Widget SongBox({required Future<void> Function(String) launchUrlCallback}) {
  const String spotifyLink =
      'https://open.spotify.com/track/6Eu31gddWw0gOGO506pJYA';

  return InkWell(
    child: Container(
      padding: const EdgeInsets.all(10),
      decoration: BoxDecoration(
        color: Colors.white10,
        borderRadius: BorderRadius.circular(10),
      ),
      child: ListTile(
        leading: ClipRRect(
  borderRadius: BorderRadius.circular(8), 
  child: Image.network(
    'https://upload.wikimedia.org/wikipedia/en/8/86/Taylor_Swift_-_Fearless.png',
    width: 60,
    height: 120,
    fit: BoxFit.cover,)),
        title: const Text('Fearless', style: TextStyle(fontWeight: FontWeight.bold)),
        subtitle: const Text('Taylor swift'),
        trailing: IconButton(
          icon: const Icon(Icons.open_in_new, color: Colors.tealAccent, size: 30),
          onPressed: () => launchUrlCallback(spotifyLink),
        ),
      ),
    ),
  );
}
Initial commit - Flutter portfolio# This is a generated file; do not edit or check into version control.

import 'package:flutter/material.dart';
